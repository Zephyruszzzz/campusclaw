# 统一换行符：仓库内一律存 LF，避免 Windows / Linux 差异导致镜像构建或脚本执行失败。
* text=auto eol=lf

# 二进制资源不做换行转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
*.zip  binary
*.db   binary
