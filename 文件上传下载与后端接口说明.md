# CampusClaw 上传 / 下载 / 后端接口说明

> 适用代码版本：当前工作区 `campusClaw`（Flask + SQLite + 私有上传目录）。
> 所有结论均以源码为准，括号内为 `文件:行号`。

---

## 一、文件上传逻辑？支持哪种文件类型？txt / md 以外是否支持？

### 1.1 结论速览

| 问题 | 答案 |
| --- | --- |
| 支持的类型 | **只有 `.txt` 和 `.md`**（`config.py:24`，`ALLOWED_EXTENSIONS = ("txt", "md")`） |
| 其他类型（PDF / Word / 图片 / 压缩包等） | **不支持，直接拒绝**，返回 `415 UNSUPPORTED_FILE_TYPE` |
| 内容编码 | 必须能被**严格 UTF-8 解码**（允许 BOM，拒绝含 `\x00`、拒绝纯空白） |
| 单文件大小 | ≤ **10 MiB**（`config.py:19`），超出返回 `413` |
| 请求体总量 | ≤ **11 MiB**（`config.py:20`，Flask `MAX_CONTENT_LENGTH`），超出由框架返回 `413` |
| 每请求文件数 | **恰好 1 个**，0 个或多个都报 `400 INVALID_FILE` |
| 谁能上传 | **只有教师**（`role == "teacher"`），学生返回 `403 ROLE_FORBIDDEN` |

### 1.2 接口

```
POST /api/materials/upload      (multipart/form-data)
```

- 请求字段（白名单，`materials.py:26` `UPLOAD_ALLOWED_FIELDS = {"file","title","csrf_token"}`）：
  - `file`：**必需**，表单字段名固定为 `file`（`materials.py:27`）
  - `title`：可选；省略时用「文件名去掉扩展名」作为标题
  - `csrf_token`：表单字段（也可以走 `X-CSRF-Token` 请求头，见 `request_context.py:88-89`）
- 成功返回：`201 {"material_id": <int>, "status": "active"}`（`material_views.py:105`）

### 1.3 判定顺序（`material_views.py:77-105` + `request_context.py:100-106`）

1. **认证**：无有效会话 → `401 AUTH_REQUIRED`
2. **角色**：非教师 → `403 ROLE_FORBIDDEN`（角色判定优先于资源是否存在）
3. **CSRF**：令牌不符 → `403 CSRF_INVALID`（失败不产生任何写入副作用）
4. **身份字段拦截**：表单里出现 `class_id / role / user_id / uploaded_by` → `400 IDENTITY_FIELD_FORBIDDEN`
   （班级、上传者一律取服务端会话，客户端传什么都不采信）
5. **未知字段**：出现白名单外的字段 → `400 INVALID_FIELDS`
6. **文件数量**：`request.files` 里除了 `file` 还有别的键，或 `file` 不止 1 个 → `400 INVALID_FILE`

### 1.4 具体校验链（业务层 `materials.py:138-224`）

按顺序执行，任一步失败即中止，**不会落库、不留文件**：

1. `sanitize_filename()`（`:94-100`）——只取 basename，剥掉目录段、`\x00`、非可打印字符；结果为空 → `400 INVALID_FILE`
2. 空文件（0 字节）→ `400 INVALID_FILE`
3. `> max_file_bytes`（10 MiB）→ `413 FILE_TOO_LARGE`
4. `detect_extension()`（`:103-110`）——取小写扩展名，**不在 `("txt","md")` 内 → `415 UNSUPPORTED_FILE_TYPE`**
5. `decode_text()`（`:119-131`）——严格 UTF-8 解码；解码失败 / 含 `\x00` / 去空白后为空 → `422 PARSE_FAILED`；同步剥掉 BOM
6. 标题校验 `validate_title()`（`:84-91`）——去首尾空白后须为 **1～200 个 Unicode 码点**且不含控制字符，否则 `400 INVALID_TITLE`

### 1.5 落盘方式（两阶段 + 同卷移动）

```
暂存写盘  →  双表同事务 INSERT  →  同卷 os.replace 移动  →  COMMIT  →  返回 id
```

- 存储键为随机 `uuid4().hex.<ext>`（`storage.py:37`），**原文件名不参与路径**，杜绝路径穿越与文件名猜测
- 暂存目录：`uploads/.staging/`；最终路径：`uploads/<class_id>/<storage_key>`（`storage.py:45`）
- 事务范围：`materials` 表 + `knowledge_entries`（正文文本）表（`materials.py:184-198`）
- 任一步失败：`ROLLBACK` + 删除暂存/已移动文件（`materials.py:219-224`），保证「要么全成，要么不留痕」

### 1.6 为什么其他类型不支持

项目定位就是「纯文本材料的班级共享」，README 明确把 **PDF / Word / 图片 / 压缩包解析**列入「不做」清单。
因此后端不仅没有对应的解析器，连扩展名白名单都只开放 `txt` / `md`——上传 `.pdf`、`.docx`、`.png`、`.zip` 会一律被挡在 `415`。

> 另外：前端 `<input type="file" accept=".txt,.md">`（`materials.html:20`）只是**浏览器侧**的友好过滤，
> 真正的强制校验在后端；直接绕过前端改扩展名也会被后端拒绝。

---

## 二、文件下载是否需要权限？实现逻辑？

### 2.1 结论

**需要权限。** 下载是**受保护接口**，不是公开静态文件：

- 必须持有**有效登录会话**（`require_api_auth`，`request_context.py:57-62`），否则返回 `401 AUTH_REQUIRED`（JSON，不会被重定向成 HTML）
- **强制班级隔离**：只能下本班材料；跨班 ID 与不存在的 ID 一样，统一 `404 MATERIAL_NOT_FOUND`（看不到"存在但无权"的差异）
- **上传目录不作为静态目录暴露**：Flask 应用 `static_folder=None`（`__init__.py:80`），
  且显式注册了 `/uploads/<key>` 与 `/static/uploads/<key>` 两个路由**直接 404**（`page_views.py:34-41`），
  防止有人靠猜路径绕过权限拿文件

### 2.2 接口

```
GET /api/materials/<material_id>/file
```

实现（`material_views.py:58-74`）：

1. `require_api_auth()` —— 会话校验（`sessions.load_session_user`，`sessions.py:47-61`：sid 摘要比对 + 已撤销检查 + 过期检查，会话 8 小时）
2. `reject_identity_fields()` —— 拒绝 query 里出现 `class_id` 等身份字段
3. **三重定位**：`WHERE id=? AND class_id=? AND status='active'`（`materials.get_active_material`，`materials.py:54-61`）
   - `class_id` 来自**服务端会话**，不是客户端
   - `status='active'` 保证已删除材料立即不可下
4. `read_material_file()` 实际读取 `uploads/<class_id>/<storage_key>`（`materials.py:332-336`）
5. 响应头：
   - `Content-Type: text/plain; charset=utf-8`
   - `Content-Disposition: attachment; filename="<原名>"`（文件名经 `_safe_download_name` 清洗 `\r\n"\/`）
   - `X-Content-Type-Options: nosniff`（防内容嗅探）

### 2.3 读全文接口（同权限模型）

```
GET /api/materials/<material_id>      → JSON，含 body_text
GET /api/materials/                   → JSON，本班材料列表
```

- 详情接口返回的 `material` 摘要**主动剔除 `storage_key`**（`material_views.py:51`），不把内部存储键暴露给前端
- 列表/详情同样要求登录，并做 `class_id` 过滤（`materials.py:42-51`），过滤在 SQL 层完成，不是"取全集再前端隐藏"

### 2.4 一个容易误会的点

页面上的「下载原文件」按钮**并不会触发浏览器下载**——它用的是 `fetch(...).then(r => r.text())`，
把文件内容渲染到页面 `<pre>` 里（`materials.html:83-87`）。
只有**直接在浏览器里访问该 URL**（或右键另存 / `curl -O`）才会真正按 `Content-Disposition` 触发下载。
所以「看到内容」和「下载到本地」是两个不同行为，权限要求完全一致。

### 2.5 删除与清理（顺带说明）

删除是「先墓碑、后删文件」两阶段（`materials.py:263-329`）：先在同事务里把 `materials.status` 置 `deleted`、
删掉 `knowledge_entries`、登记一条 `file_cleanup_jobs`；提交后再删磁盘文件。
若磁盘删除失败（如权限不足），任务留 `pending`，由启动恢复或
`python scripts/cleanup_files.py --once` 重试。删除后接口立刻返回 `404`，**不等文件删完**。

---

## 三、后端（API）是否暴露给用户？如何用浏览器直接访问？

### 3.1 是否暴露

**是，API 就在同一个 Web 服务的同一个端口上，对能访问该端口的人可见**，没有独立的 API 域名、没有 API Key、也没有"隐藏接口"的设计。但要区分两层含义：

| 维度 | 情况 |
| --- | --- |
| **网络可达性** | API 与页面同源同端口（默认 `0.0.0.0:8080`） |
| **访问控制** | 靠 **会话 Cookie + 角色 + CSRF** 保护，不靠"藏起来" |
| **匿名可访问的接口** | 只有 `GET /health`（`health_views.py:13-21`，返回 `{"status":"ok"}`） |
| **跨域调用** | 未开启 CORS，其他站点的 JS **读不到**接口返回（只能同源/自己带 Cookie 调） |
| **身份字段** | `class_id / role / user_id / uploaded_by` 一律以服务端会话为准，客户端传了直接 `400` |

接口清单：

| 方法 | 路径 | 权限 | 成功码 |
| --- | --- | --- | --- |
| GET | `/health` | 匿名 | 200 / 503 |
| GET | `/login`、`POST /login` | 匿名（有 CSRF 保护） | 302 |
| POST | `/logout` | 任意已登录 | 302 |
| GET | `/`、`/materials` | 已登录（未登录 302 跳登录页） | 200 |
| GET | `/api/materials/` | 已登录 | 200 |
| GET | `/api/materials/<id>` | 已登录 + 本班 | 200 |
| GET | `/api/materials/<id>/file` | 已登录 + 本班 | 200（附件） |
| POST | `/api/materials/upload` | 教师 + CSRF | 201 |
| PATCH | `/api/materials/<id>` | 教师 + CSRF | 200 |
| DELETE | `/api/materials/<id>` | 教师 + CSRF | 204 / 202 |

统一错误结构：`{"error":{"code":"...","message":"..."}}`（`errors.py:25-26`）。
注意：`/api/...` 与 `/health` 出错**永远返回 JSON**，不会被 302 重定向成登录页 HTML（`request_context.py:133-145`）。

### 3.2 用浏览器直接访问的正确姿势

**核心前提：GET 类接口靠 Cookie 认证，所以必须先在该浏览器里登录，再访问接口 URL。**

**① 公开接口——地址栏直接敲**

```
http://127.0.0.1:8080/health
```
无需登录，直接看到 `{"status":"ok"}`。

**② 需登录的 GET 接口——先在 `/login` 登录，再敲地址栏**

```
http://127.0.0.1:8080/api/materials/
http://127.0.0.1:8080/api/materials/1
http://127.0.0.1:8080/api/materials/1/file     ← 这一条会直接触发浏览器下载
```

- 未登录时敲这些 URL，返回的是 `401` 的 JSON（`{"error":{"code":"AUTH_REQUIRED",...}}`），**不会**跳登录页——这是有意设计
- 登录后同一浏览器带着会话 Cookie 再敲，就能看到 JSON / 触发下载
- 直接敲 `/api/materials/1/file` 是**唯一能真正触发"下载文件"**的浏览器方式（页面按钮走的是 fetch 预览）

**③ 写操作（POST / PATCH / DELETE）——地址栏做不到**

地址栏只能发 GET，且写接口需要 `X-CSRF-Token` 请求头。两种可用办法：

- **打开页面 F12 → Console**，在已登录状态下执行（token 从页面里取）：

```js
// 先拿到页面里的 CSRF token（登录后任意受保护页面都有）
const csrf = document.querySelector('input[name=csrf_token]').value;

// 改标题
fetch('/api/materials/1', {
  method: 'PATCH',
  headers: {'Content-Type': 'application/json', 'X-CSRF-Token': csrf},
  body: JSON.stringify({title: '新标题'})
}).then(r => r.json()).then(console.log);

// 删除
fetch('/api/materials/1', {
  method: 'DELETE',
  headers: {'X-CSRF-Token': csrf}
}).then(r => console.log(r.status));
```

- **或用 `curl`（需要自己带 Cookie 与 token）**——先登录拿 Cookie，再从页面复制 token，最后：

```bash
# 1) 登录（-c 保存 Cookie，登录表单需带 csrf_token）
curl -i -c cookie.txt http://127.0.0.1:8080/login
# 2) 从登录页 HTML 里取出 csrf_token，然后：
curl -i -b cookie.txt -c cookie.txt \
  -d 'username=teacher_a&password=TeacherA#2026&csrf_token=<TOKEN>' \
  http://127.0.0.1:8080/login
# 3) 带 Cookie 调受保护接口
curl -i -b cookie.txt http://127.0.0.1:8080/api/materials/
curl -i -b cookie.txt -o out.txt http://127.0.0.1:8080/api/materials/1/file
```

- **上传**用浏览器表单最省事（页面已有上传卡片）；脚本上传时记得带 `-F file=@x.txt`、`-F csrf_token=...`、以及 Cookie。

**④ 需要留意的行为差异**

- 用地址栏访问 `/api/...` 一旦返回 `401`，浏览器不会帮你跳登录页，只会显示 JSON —— 这是 API 的预期行为
- 会话 8 小时，过期后所有受保护接口都会回到 `401`，重新登录即可
- 如果开启 `AUTH_COOKIE_SECURE=1`，则只有 HTTPS 下 Cookie 才会下发，纯 HTTP 地址栏访问会一直 401

### 3.3 部署层面的提醒（安全相关）

- 默认的 `SECRET_KEY` 与测试账号口令是**公开内置值**，仅供本机演示；对外部署务必设置
  `CAMPUSCLAW_STRICT_CONFIG=1` 并提供真实 `SECRET_KEY` 与 `SEED_*` 口令（否则进程会拒绝启动）。
- 服务默认监听 `0.0.0.0:8080`，即**同网段可访问**。若要对外暴露，应放在反向代理/内网之后，
  不要直接把带内置凭据的实例挂到公网。

---

## 四、一句话总结

- **上传**：`POST /api/materials/upload`，教师 + CSRF，**只收单个 UTF-8 的 `.txt` / `.md`**，≤10 MiB；其他类型一律 `415`。
- **下载**：`GET /api/materials/<id>/file`，**必须登录且限本班**，上传目录不对外做静态路由；页面按钮只是前端预览，真正下载需直接访问该 URL。
- **API**：与页面同源同端口、对用户可见，但受「会话 Cookie + 角色 + CSRF + 班级隔离」保护；匿名仅 `/health`。浏览器里**先登录**，GET 可直接敲地址栏，写操作需在 Console / curl 里带上 `X-CSRF-Token`。
